<Window x:Class="RevitSpoolCopy.UI.SpoolManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Spool Manager" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F0F0F0">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Manage All Spools in Model" 
                   FontSize="14" FontWeight="Bold" Margin="0,0,0,12"/>

        <!-- Spool List -->
        <DataGrid Grid.Row="1" Name="SpoolDataGrid" Margin="0,0,0,12"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Spool Name" Binding="{Binding SpoolName}" Width="*"/>
                <DataGridTextColumn Header="Part Count" Binding="{Binding PartCount}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Selection -->
        <GroupBox Grid.Row="2" Header="Action" Margin="0,0,0,12" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <RadioButton Grid.Row="0" Grid.Column="0" Name="RenameRadio" 
                             Content="Rename selected spool to:" Margin="0,6" 
                             Checked="RenameRadio_Checked"/>
                <TextBox Grid.Row="0" Grid.Column="1" Name="RenameTextBox" 
                         Padding="8" Margin="8,6" IsEnabled="False"/>

                <RadioButton Grid.Row="1" Grid.Column="0" Name="DeleteRadio" 
                             Content="Delete selected spool (clear parts)" Margin="0,6"
                             Checked="DeleteRadio_Checked"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                           Text="Select a spool above, then choose an action." 
                           Foreground="Gray" FontStyle="Italic" Margin="0,12,0,0"/>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="ApplyButton" Content="Apply" 
                    Padding="16,8" Margin="6,0" Click="ApplyButton_Click"
                    Background="#007ACC" Foreground="White" FontWeight="Bold"/>
            <Button Name="CancelButton" Content="Cancel" 
                    Padding="16,8" Margin="6,0" Click="CancelButton_Click"
                    Background="#E0E0E0" Foreground="Black"/>
        </StackPanel>
    </Grid>
</Window>
